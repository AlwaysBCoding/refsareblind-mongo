%h1 USER ACCOUNT PAGE

.account-details
	%p= @current_user
	%p= @current_user.email

.pools
	- @current_user.entries.each do |entry|
		.entry
			= link_to "#{entry.name} - #{entry.pool.name}", pool_url(entry.pool.slug)
